import {StyleSheet} from 'react-native';

// a common text style meant to be used in every text component
const styles = StyleSheet.create({
  textCommon: {
    fontFamily: 'Gill Sans',
  },
});

export const textStyleCommon = styles.textCommon;
